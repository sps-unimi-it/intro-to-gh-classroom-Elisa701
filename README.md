# Elisa’s Exam — Replication Study: Sentiment Dictionaries on BBC News

Repository for the final project of the c4ss (Computational Social Science) course.
This repository holds the completed project of the Computational Social Science class. In this project, a replication of the proposed methodology by Chan et al. (2021) has been done by using three sentiment dictionaries, which are AFINN, Bing Liu, and NRC on a balanced set of 90 articles from BBC news. The main idea behind this study is to compare the sentiments through different lexicons and effect of normalization of scores on document-level sentiments.


## Project Structure

```text
Esame_Elisa/
├── Data/
│   └── BBC_news/              # Corpus of 90 .txt articles
│       ├── health/
│       │   ├── health_01.txt
│       │   ├── health_02.txt
│       │   ├── ...
│       │   └── health_30.txt
│       ├── sport/
│       │   ├── sport_01.txt
│       │   ├── sport_02.txt
│       │   ├── ...
│       │   └── sport_30.txt
│       └── tech/
│           ├── tech_01.txt
│           ├── tech_02.txt
│           ├── ...
│           └── tech_30.txt
│
├── Esame_Elisa_BBC.ipynb      # Main notebook with the analysis
├── lexica/                    # Local mirror/fallback copies of the lexicons
│   ├── AFINN-111.txt
│   ├── bing-positive-words.txt
│   ├── bing-negative-words.txt
│   └── NRC-Emotion-Lexicon-Wordlevel-v0.92.txt
├── Results/                  # CSV files and figures generated by the analysis
│   ├── sentiment_by_article.csv
│   ├── sentiment_by_category.csv
│   ├── boxplot_sentiment_by_category.png
│   ├── dictionary_agreement.png
│   └── content_length_bias.png
└── README.md                 # This file (rename README_v2.md → README.md)
```

Each .txt file contains a `Title:` line, a `URL`: line, and the article body, following the structure of `Data/Presidents_texts/`.
Files are organized into subfolders by category (`health/`, `sport/`, `tech/`).


## Methodology
The notebook follows the analytical workflow proposed by Chan et al. (2021):

- collect a balanced news corpus;
- preprocess textual data;
- compute sentiment using three off-the-shelf dictionaries;
- calculate both raw and normalized sentiment scores;
- compare dictionary agreement;
- evaluate the influence of document length on sentiment estimation.


## Lexicons Used

The three off‑the‑shelf dictionaries required for the exam are:

1. **AFINN-111** —  integer scores from −5 to +5 per word.
2. **Bing Liu** — lists of positive and negative words.
3. **NRC Emotion Lexicon** — associations of words with 8 emotions, including positive and negative.

The notebook looks for lexicons in `../downloaded_lexica/` (as in the original course structure).
If that folder is not available, it automatically falls back to the local copies in `Esame_Elisa/lexica/`.


## Requirements

You only need Python 3 installed. Required libraries are:

- `pandas`
- `numpy`
- `matplotlib`
- `scipy`
- `jupyter`


## Produced output

| File | Description |
|------|-------------|
| `sentiment_by_article.csv` | Raw and normalized scores for each article |
| `sentiment_by_category.csv` | Average normalized scores per category |
| `boxplot_sentiment_by_category.png` | Distribution of normalized scores by lexicon and category |
| `dictionary_agreement.png` | Correlations between the three dictionaries |
| `content_length_bias.png` | Comparison between raw (length‑dependent) and normalized scores |


## Submission via GitHub Classroom

1. **Accept the assignment** using the GitHub Classroom link provided by the instructor.
2. **Clone** the personal repository automatically created by GitHub Classroom:

   ```bash
   git clone <URL-del-tuo-repo>
   cd <nome-repo>
   ```

3. **Organize the files** according to the exam instructions. Make sure the following are present:

   - `Esame_Elisa/Data/BBC_news/*`
   - `Esame_Elisa/Esame_Elisa_BBC.ipynb`
   - `Esame_Elisa/Results/*`
   - Lexicons in `downloaded_lexica/` (if using the original course structure) or the local copies          in  `Esame_Elisa/lexica/`


4. **Committ e push** everything:

   ```bash
   git add .
   git commit -m "Consegna esame c4ss — BBC sentiment analysis"
   git push origin main
   ```


5. **Check** on GitHub that all files were uploaded correctly and that the notebook renders properly.


## Notes

- Articles were downloaded from official BBC URLs and saved as  `.txt` files in `Esame_Elisa/Data/BBC_news/`.
- The analysis uses **length‑normalized scores** (`score / word_count`)to replicate the content‑length bias correction discussed in the reference study.
- Sport is the most positive category on average, health is the most negative while technology is neutral or slightly positive depending on the referred lexicon.
- Generally, the goal of the project is to emulate the methodology recommended by Chan et al. (2021), rather than duplicate the original data set, in order to show how various sentiment lexicons function on professionally written news articles.
